import { Outlet } from 'react-router-dom';
import './App.css';
import { Box } from '@mui/system';

function App() {
  return (
   <Box>
      Reactivity REACT REDUX
      Reactivity REACT REDUX 
      Reactivity REACT REDUX 
      Reactivity REACT REDUX 
      Reactivity REACT REDUX
      <Outlet />
   </Box>
     
  );
}

export default App;
